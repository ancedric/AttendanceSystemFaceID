<Window x:Class="Home"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartATSFaceID"
        mc:Ignorable="d"
        Title="Home" Height="768" Width="1024"
        WindowStartupLocation="CenterScreen">
    <Grid Loaded="Grid_Loaded">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1023*"/>
        </Grid.ColumnDefinitions>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <LinearGradientBrush.RelativeTransform>
                    <TransformGroup>
                        <ScaleTransform CenterY="0.5" CenterX="0.5"/>
                        <SkewTransform CenterX="0.5" CenterY="0.5"/>
                        <RotateTransform Angle="-51" CenterX="0.5" CenterY="0.5"/>
                        <TranslateTransform/>
                    </TransformGroup>
                </LinearGradientBrush.RelativeTransform>
                <GradientStop Color="#FF041367"/>
                <GradientStop Color="#FF556DEF" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <StackPanel Grid.Column="1" Margin="8,140,770,0">
            <Button x:Name="homeBtn" Content="Accueil"  
                    Tag="HomeControl1" 
                    Click="MenuButton_Click" Height="78" BorderBrush="#FFF5F3F3" Background="{x:Null}" Foreground="White" FontFamily="Century Gothic" FontSize="14" Margin="0,0,0,21" Cursor="Hand"/>
            <Button x:Name="employeBtn" Content="Employes" 
                    Tag="EmployeControl" Height="80" Background="{x:Null}" BorderBrush="#FFFDFBFB" Foreground="White" FontFamily="Century Gothic" FontSize="14" Margin="0,0,0,10"/>
            <Button x:Name="rapportBtn" Content="Rapport" 
                Tag="ReportControl" Height="80" Background="{x:Null}" BorderBrush="#FFFDFBFB" Foreground="White" FontFamily="Century Gothic" FontSize="14"/>

        </StackPanel>
        <ContentControl 
                        x:Name="ContentArea" 
                        Margin="252,0,0,0" Grid.Column="1"/>
    </Grid>
</Window>
